import NichePage from "@/components/niche-page/NichePage";
import { dentalConfig } from "./config";

const DentalMarketingPage = () => {
  return <NichePage config={dentalConfig} />;
};

export default DentalMarketingPage;
