import NichePage from "@/components/niche-page/NichePage";
import { medSpaConfig } from "./config";

const MedSpaMarketingPage = () => {
  return <NichePage config={medSpaConfig} />;
};

export default MedSpaMarketingPage;
